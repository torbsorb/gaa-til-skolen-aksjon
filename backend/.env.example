# Local development environment (Postgres-first)
APP_MODE=preview
DATABASE_URL=postgresql://gotilskole:gotilskole@localhost:5432/gotilskole
# Optional override; auto-default is disable on localhost and require on remote hosts
# PGSSLMODE=disable
